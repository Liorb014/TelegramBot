package org.example;

import org.telegram.telegrambots.meta.api.objects.replykeyboard.buttons.InlineKeyboardButton;

import javax.swing.*;

public class UsersStatistics extends JPanel {
    private int requestsSoFar;
    private int differentUsersAmount;
    private Long mostActiveUser;
    private InlineKeyboardButton mostPopularActivity;

    public UsersStatistics() {
   //    this.differentUsersAmount = MessagesBot.chatIds.size();
//       this.requestsSoFar =
    }
}
